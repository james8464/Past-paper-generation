[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "examforge-aqa-accounting"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = ["pydantic>=2", "reportlab>=4"]
