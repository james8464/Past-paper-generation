[project]
name = "aqa-economics-practice-generator"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.0",
  "reportlab>=4.0"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["aqaecongen*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
